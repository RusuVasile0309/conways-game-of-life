# macOS
.DS_Store

# Node
node_modules/

# Claude Code local settings (per-user; references absolute paths)
.claude/settings.local.json

# Build outputs
dist/
out-tsc/
.next/
test-output/

# Nx cache and generated workspace data
.nx/

# Next.js generated files
apps/web/next-env.d.ts

# SQLite database (generated at runtime; migrations are committed, binary is not)
api/data/*.db
